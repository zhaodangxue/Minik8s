kind: Service
apiVersion: v1
metadata:
  name: dns-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - name: service-port1
      port: 22222
      targetPort: hello
      protocol: TCP 